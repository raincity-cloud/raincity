// Auto-generated by smithy-gen. Do not edit.
import { z } from "zod/v4";

/**
 * ```xml
 * Concrete vendor params to apply to AWS services by default.
 * ```
 */
export const awsVendorParamsSchema = z.object({});
/**
 * ```xml
 * Base vendor params for all aws services.
 * ```
 */
export const baseAwsVendorParamsSchema = z.object({
  /**
   * ```xml
   * The AWS region to sign the request for and to resolve the default
   * endpoint with.
   * ```
   */
  // TODO: structure member target smithy.api#String for BaseAwsVendorParams.region is not generated yet.
  region: z.unknown().optional(),
  /**
   * ```xml
   * The set of regions to sign a sigv4a request with.
   * ```
   */
  // TODO: structure member target aws.test#NonEmptyStringList for BaseAwsVendorParams.sigv4aRegionSet is not generated yet.
  sigv4aRegionSet: z.unknown().optional(),
  /**
   * ```xml
   * A static endpoint to send the request to.
   * ```
   */
  // TODO: structure member target smithy.api#String for BaseAwsVendorParams.uri is not generated yet.
  uri: z.unknown().optional(),
  /**
   * ```xml
   * Whether to resolve a FIPS compliant endpoint or not.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for BaseAwsVendorParams.useFips is not generated yet.
  useFips: z.unknown().optional(),
  /**
   * ```xml
   * Whether to resolve a dualstack endpoint or not.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for BaseAwsVendorParams.useDualstack is not generated yet.
  useDualstack: z.unknown().optional(),
  /**
   * ```xml
   * Whether to use account ID based routing where applicable.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for BaseAwsVendorParams.useAccountIdRouting is not generated yet.
  useAccountIdRouting: z.unknown().optional(),
});
/**
 * ```xml
 * Vendor params for S3.
 * ```
 */
export const s3VendorParamsSchema = z.object({
  /**
   * ```xml
   * Whether to resolve an accelerate endpoint or not.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for S3VendorParams.useAccelerate is not generated yet.
  useAccelerate: z.unknown().optional(),
  /**
   * ```xml
   * Whether to use the global endpoint for us-east-1.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for S3VendorParams.useGlobalEndpoint is not generated yet.
  useGlobalEndpoint: z.unknown().optional(),
  /**
   * ```xml
   * Whether to force path-style addressing.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for S3VendorParams.forcePathStyle is not generated yet.
  forcePathStyle: z.unknown().optional(),
  /**
   * ```xml
   * Whether to use the region in the bucket arn to override the set
   * region.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for S3VendorParams.useArnRegion is not generated yet.
  useArnRegion: z.unknown().optional(),
  /**
   * ```xml
   * Whether to use S3's multi-region access points.
   * ```
   */
  // TODO: structure member target smithy.api#Boolean for S3VendorParams.useMultiRegionAccessPoints is not generated yet.
  useMultiRegionAccessPoints: z.unknown().optional(),
});
