// Auto-generated by smithy-gen. Do not edit.
import { z } from "zod/v4";
import { nonEmptyStringListSchema } from "./aws.test.schema.js";

/**
 * ```xml
 * Concrete vendor params to apply to AWS services by default.
 * ```
 */
export const awsVendorParamsSchema = z.object({});

/**
 * ```xml
 * Base vendor params for all aws services.
 * ```
 */
export const baseAwsVendorParamsSchema = z.object({
  /**
   * ```xml
   * The AWS region to sign the request for and to resolve the default
   * endpoint with.
   * ```
   */
  region: z.string().optional().default("us-west-2"),
  /**
   * ```xml
   * The set of regions to sign a sigv4a request with.
   * ```
   */
  sigv4aRegionSet: nonEmptyStringListSchema.optional(),
  /**
   * ```xml
   * A static endpoint to send the request to.
   * ```
   */
  uri: z.string().optional(),
  /**
   * ```xml
   * Whether to resolve a FIPS compliant endpoint or not.
   * ```
   */
  useFips: z.boolean().optional().default(false),
  /**
   * ```xml
   * Whether to resolve a dualstack endpoint or not.
   * ```
   */
  useDualstack: z.boolean().optional().default(false),
  /**
   * ```xml
   * Whether to use account ID based routing where applicable.
   * ```
   */
  useAccountIdRouting: z.boolean().optional().default(true),
});

/**
 * ```xml
 * Vendor params for S3.
 * ```
 */
export const s3VendorParamsSchema = z.object({
  /**
   * ```xml
   * Whether to resolve an accelerate endpoint or not.
   * ```
   */
  useAccelerate: z.boolean().optional().default(false),
  /**
   * ```xml
   * Whether to use the global endpoint for us-east-1.
   * ```
   */
  useGlobalEndpoint: z.boolean().optional().default(false),
  /**
   * ```xml
   * Whether to force path-style addressing.
   * ```
   */
  forcePathStyle: z.boolean().optional().default(false),
  /**
   * ```xml
   * Whether to use the region in the bucket arn to override the set
   * region.
   * ```
   */
  useArnRegion: z.boolean().optional().default(true),
  /**
   * ```xml
   * Whether to use S3's multi-region access points.
   * ```
   */
  useMultiRegionAccessPoints: z.boolean().optional().default(true),
});
