// Auto-generated by smithy-gen. Do not edit.
import { z } from "zod/v4";
import { stringListSchema } from "./smithy.protocols.schema.js";

/**
 * ```xml
 * An RPC-based protocol that serializes CBOR payloads.
 * ```
 */
export const rpcv2CborSchema = z.object({
  /**
   * ```xml
   * Priority ordered list of supported HTTP protocol versions.
   * ```
   */
  http: stringListSchema.optional(),
  /**
   * ```xml
   * Priority ordered list of supported HTTP protocol versions
   * that are required when using event streams.
   * ```
   */
  eventStreamHttp: stringListSchema.optional(),
});
export type Rpcv2Cbor = z.infer<typeof rpcv2CborSchema>;
